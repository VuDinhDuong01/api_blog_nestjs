// import { HttpException } from "@nestjs/common";

// class BaseException extends HttpException{
//     constructor({status, message, name, stack}:{status?: number, message?: string, name?: string , stack?: string }){
//         super(status,message, name)
//     }
// }